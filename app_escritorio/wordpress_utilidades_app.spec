# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['wordpress_utilidades_app.py'],
    pathex=[],
    binaries=[],
    datas=[('app-docs.html', '.'), ('..\\version_bat\\docker-wordpress-docs.html', 'version_bat')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='Wordpress_Utilidades',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    version='version_info.py',
)
